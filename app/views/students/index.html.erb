<div class="row">
  <% if flash[:notice] != nil %>
    <div class= "alert alert-success col-lg-offset-4 col-lg-4 centralize">
      <a href="#" class="close" data-dismiss="alert" aria-label="close">&times;</a>
      <p id="notice"><%= notice %></p>
    </div>
  <%end%>
</div>
<div class="row form-group">
  <%= link_to "Cadastrar Aluno", new_student_path, class: "btn btn-primary" %>
</div>
<div class="row">
  <table id="students-table" data-source="<%= students_path(format: :json) %>" class="table table-striped table-bordered display responsive no-wrap">
    <thead>
      <tr>
        <th>Nome:</th>
        <th>Gênero:</th>
        <th>Nº Nis:</th>
        <th>Turma:</th>
      </tr>
    </thead>
    <tbody>
    </tbody>
  </table>
</div>